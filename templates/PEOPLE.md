<people>

# People

<!--
## Display Name
- Discord ID: 123456789012345678
- 別名: ["Nickname", "Another name"]

(free-form notes below)

Rules:
- One `##` section per person. The heading is used for name matching.
- `Discord ID` must use exactly this field name with a numeric value, or omit the line.
- `別名` (aliases) should be a JSON string array of names people actually use in
  conversation. Old delimiter formats (／ / ()) are accepted but new entries should
  use the array form.
- Everything else is free-form Markdown — personality, relationship, permissions,
  history, whatever is useful.
- Do not add the owner here; owner information belongs in OWNER.md.
-->

</people>
